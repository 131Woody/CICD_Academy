name: Build Docker image
description: build docker image and list all docker images

runs:
  using: composite
  steps:
    - name: Build the Docker image
      run: |
        docker build . --file Dockerfile --tag car-app-list:$(date +%s)
        docker image ls
      shell: bash