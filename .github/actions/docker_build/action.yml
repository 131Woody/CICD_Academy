name: Build Docker image
description: {{ action-description }}

runs:
  using: composite
  steps:
    - name: Build the Docker image
      run: |
        docker build . --file Dockerfile --tag car-app-list:$(date +%s)
        docker image ls
